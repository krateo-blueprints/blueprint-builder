{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Blueprint Builder",
  "description": "Replace this with what the blueprint provisions. This text is what an author reads above the create form.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name"],
  "properties": {
    "name": {
      "type": "string",
      "title": "Name",
      "description": "Name of the thing being provisioned. Also used to derive resource names.",
      "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$"
    },
    "replicas": {
      "type": "integer",
      "title": "Replicas",
      "description": "How many replicas to run.",
      "default": 1,
      "minimum": 1
    }
  }
}
